import * as React from 'react';
import { mount, ReactWrapper } from 'enzyme';
import { Action, Dispatch } from 'redux';
import { StatelessComponent } from 'react';

describe('connectReduxUIState', () => {
  it('', () => {
    expect(true).toBe(true);
  });
});
